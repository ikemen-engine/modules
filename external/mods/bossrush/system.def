; Loaded automatically from external/mods/**/system.def and merged before the motif system.def.
; The motif file is loaded last, so anything defined there overrides this file.
; Avoid defining menu.itemname.* here (put it in the main system.def to preserve menu order).

[Title Info]
	; You need to add itemname to the main system.def so menu items keep the expected order. Grouping rules:
	; https://github.com/ikemen-engine/Ikemen-GO/wiki/Screenpack-features#grouping-elements-submenus
;menu.itemname.bossrush = "BOSS RUSH"

[Select Info]
title.bossrush.text = "Boss Rush"
record.bossrush.text = "- BEST RECORD: %n -\n%c: Round %r"

[Hiscore Info]
	; Set per-gamemode ranking criteria: score|time|win. Used to sort hiscores for that mode.
ranking.bossrush = "win"
title.bossrush.text = "Ranking Boss Rush"

[Win Screen]
	; Set per-gamemode win/results screen variant to use for that mode.
	; Use "Win Screen" (default) or a specific "<X> Results Screen" name.
results.bossrush = "Boss Rush Results Screen"

[Boss Rush Results Screen]
enabled = 1
sounds.enabled = 1

roundstowin = 1

fadein.time = 32
fadein.col = 0, 0, 0
fadein.anim = -1
fadein.snd = -1, 0

fadeout.time = 64
fadeout.col = 0, 0, 0
fadeout.anim = -1
fadeout.snd = -1, 0

show.time = 300
state.time = 0

winstext.font = enter48.def, 0, 0
winstext.offset = 640,240
winstext.scale = 1.0, 1.0
winstext.xshear = 0
winstext.angle = 0
winstext.text = Bosses defeated: %i
winstext.layerno = 2
winstext.window = 
;winstext.localcoord = 

winstext.displaytime = 0

overlay.col = 0, 0, 0
overlay.alpha = 0, 128
overlay.window = 
;overlay.localcoord = 

cancel.key = a, b, c, x, y, z, s

p1.state = 175, 170
p1.win.state = 180
p1.teammate.state = 
p1.teammate.win.state = 

p2.state = 
p2.win.state = 
p2.teammate.state = 
p2.teammate.win.state = 

[BossRushResultsBGdef]
; Left blank (character and stage not covered)
